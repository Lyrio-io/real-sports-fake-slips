# Real Sports Fake Slips

Sandbox sports betting app — practice with fake money, keep your real money.

## Run locally
```
npm install
npm start
```
Then open http://localhost:8080

## Deploy
Railway auto-detects `package.json` and runs `npm start` on port 8080.
